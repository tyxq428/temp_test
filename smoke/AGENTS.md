# Smoke-directory Agent Instructions

These instructions apply to work performed in `smoke/`.

1. Read `input.txt` and update only `output.txt`.
2. `output.txt` must contain exactly one line in the form `result=<integer>` followed by a newline.
3. Do not modify `input.txt` or this instruction file.
4. The final structured result must set `scoped_policy_ack` to exactly `SMOKE_POLICY_V1`.
5. Report the task-authored changed file as `output.txt`.
